Q: заполнение .xlt (MS Excel template) из Java?

Response1
я их таким образом не заполнял а читал
sun.jdbc.odbc.JdbcOdbcDriver
работало, только была хитрость с именами
Пример "Select * from [Price$]" то есть надо было вот так имя Sheet оборачивать (кавычками и долларом)
через odbc мост можно только читать, к сожалению


Response2
а чем радикально отличается XLS и XLT ?
Если ничем особенным (то есть тебе функционал workbook`a нужен), то могу предложить IBM Excel Accessor. 
Я с ним работал - но там есть определеные трудности.
Трудности в основном из-за перформанса, - если парсить структуру xls`а например - ну и вообще 
код не очень здоровский получается, но работает.


Response3
by P
смотри сюда, там практически всё написано -
http://users.rcn.com/danadler/javacom/
(но это для того случая, когда рядом есть машина с установленным Excel)









******************************

more responses:

Response
мне не надо интерактивно с пользователем работать
Иначе - это заюзать что-то, что понимается Excel, но не зависит от него. Например, через HTML.
Т.е. мне надо на сервере поменять/добавить что-то в файлик, и заслать его юзеру. А тот его откроет в Excel.


Response
P 
Тогда csv
> Иначе - это заюзать что-то, что понимается Excel, но не зависит от него. 
> Например, через HTML.
> Т.е. мне надо на сервере поменять/добавить что-то в файлик, и заслать его 
> юзеру. А тот его откроет в Excel.
Или просто tdt. Можно кстати и XML, только не всякий Ёксель его поймёт.


Response
csv не катит, так как надо RTF + макросы
В HTML вроде как все это сохраняется, т.е. создаешь .xlt со всеми наворотами, сохраняешь как HTML, и в таком виде хранишь на сервере.
При запросе вставляешь нужные данные и отдаешь HTML. А пользователь открывает это в excel.
Это один из вариантов, и вроде должно работать. Возможно, вместо HTML можно что-то еще.
Иначе, действительно, надо Windows сервер с установленным Excel.


Response (unrelated?)
В HTML Excel по разному сериализует в зависимости от версии :-(


Response
P 
так нужны бантики или данные? (+)
берёшь xsl, который при первой загрузке (при открытии себя) с помощью макроса заполняет себя из csv и наводит красоту. 
Файл пусть берёт из той-же директории, где сам лежит. Клиенту отправляешь ёксель + данные. И все радуются. Хотя коряво
